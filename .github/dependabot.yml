version: 2
updates:
  - package-ecosystem: 'npm'
    # Files stored in `app` directory
    directory: '/'
    schedule:
      interval: 'daily'
    open-pull-requests-limit: 10
    groups:
      all-dependencies:
        patterns:
          - '*'
    target-branch: 'main'
    ignore:
      - dependency-name: 'storybook'
      - dependency-name: '@storybook/*'
      # TypeScript 7 is blocked until Next.js and typescript-eslint support its compiler API.
      - dependency-name: 'typescript'
        update-types: ['version-update:semver-major']
